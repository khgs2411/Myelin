# List project schema candidates by default

`schema candidates <project>` lists project-local schema candidates by default. Global schema candidates are higher-impact and should be surfaced intentionally with `--include-global`. This keeps day-to-day schema review focused while still allowing operators and agents to inspect global schema work relevant to a project.
