# Implement schema before project learn

The TypeScript migration should implement schema functionality before `project learn`. Learning depends on schema-context freshness, review gates, taxonomy rules, provenance requirements, and schema candidate handling. Building `project learn` first would bake in behavior before the maintenance contract exists. Schema commands and runtime modules should therefore land before the learning command is wired for normal use.
