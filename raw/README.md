# Raw Intake

This directory is the top-level intake area for unorganized source material.

- `inbox/`: newly dumped files that have not been processed yet
- `processed/`: preserved original files that were successfully ingested
- `rejected/`: files that were intentionally not ingested

Nothing in `raw/inbox/` should be treated as canonical project knowledge.
