# Hand-author typed schema JSON first

Typed schema rules should be hand-authored JSON files at first. Markdown files carry the explanatory intent, while JSON files encode enforceable rules. A higher-level rule authoring format or generator would add another compiler layer before the need is proven. If direct JSON authoring becomes painful or duplicative, a later design can introduce generation from a richer source format.
