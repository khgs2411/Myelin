# Operator CLI And Workflows

Placeholder for the subject writer.

Document the active Myelin CLI surface from an operator perspective. Include command vocabulary, side effects, read-only versus write behavior, project bootstrap, status, schema check/build, `project learn`, top-level `ingest`, memory query/index/inbox commands, install/capture/session commands, Makefile aliases, and important deprecated or absent commands such as `project ingest`. Explain normal development and verification commands.

Suggested repo paths to inspect: `docs/CLI.md`, `README.md`, `AGENTS.md`, `Makefile`, `src/cli.ts`, `src/commands/registry.ts`, `src/commands/status.ts`, `src/commands/bootstrap.ts`, `src/commands/project.ts`, `src/commands/ingest.ts`, `src/commands/memory.ts`, `src/commands/schema.ts`, `src/commands/session.ts`, `src/commands/capture.ts`, `src/commands/install.ts`, `tests/commands/`.
