# Use markdown guidance and typed schema rules

The schema layer should include both markdown guidance and typed machine-readable rules. Markdown carries human and agent-readable intent. Typed JSON or YAML rules give validators deterministic contracts for page taxonomy, review gates, allowed memory scopes, required provenance fields, CLI vocabulary, and validation requirements. This keeps the wiki expressive while giving the runtime enforceable boundaries.
