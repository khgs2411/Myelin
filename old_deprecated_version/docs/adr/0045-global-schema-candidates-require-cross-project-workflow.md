# Require cross-project workflow for global schema candidates

`project learn <project>` does not generate global schema candidates. A single project can suggest project-local schema, but global schema affects every project and should require explicit cross-project workflow, operator intent, or later Practice/Personal promotion logic backed by cross-project evidence.
