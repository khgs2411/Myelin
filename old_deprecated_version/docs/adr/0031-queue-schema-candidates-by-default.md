# Queue schema candidates by default

`project learn` may discover project-local schema conventions from repeated project evidence, but schema changes affect future agent behavior and need a higher bar than routine Project Memory updates. Schema candidates are queued by default. Only narrow additive project-local conventions with high confidence may auto-apply. Weakening or replacing global rules still requires an explicit typed override with a reason.
