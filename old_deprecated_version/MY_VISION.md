I will describe how I see myself using a Codex agent during a conversation, how I and the agent would use LLMWiki in this repo during normal day‑to‑day work. I assume we have an MCP layer that exposes functions, and the only function that matters for this vision of the product is the query function from the MCP layer. So we are working with the assumption that we will have a working MCP layer in the future, and it has a query function.

I would start a new session and essentially tell the agent, “Let’s work on X and Y.” I will have this as another assumption that we are allowed to make. I will have a skill that instructs the agent to load it by default; that skill is bound to myelin as well.
What that skill does is tell the agent to use the MCP tool and fetch the recent session memory. That's how every new session would start. It would teach the agent on the memory layer, on the second brain we have created, about the MCP and how to use it and would instruct it to fetch the recent session memory so that every agent starts his run with essentially a memory of what we did earlier. I have my own ways to make a skill be triggered by the agent by default. I saw another library do it and I'm going to replicate that behavior. So we can assume that every agent would start by querying the session memory using the MCP to retrieve recent memory always.
Which also means that the agent would then be aware of the existence of myLM, the MCP tool, and how to use it, and will know when to query the brain for information. That's the assumption we need to have, and that's the behavior that the session memory is going to give us. Not only that, the agent can, of course, once he is aware of the existence of the session memory, always query it for recent information. That's the session memory layer.
The MCP will also expose a memory of type `project` which the agent can query and will be instructed to query when working on features or searching information on features in the project. Let's say I want to ask an agent working on this project: "where is the SQLite database stored in myelin for a project session memory?"
The agent would then be able to query using the MCP and pass an argument of type project, which will then query the SQLite index of the project, returning relevant documentation that matches the specific question. What that means, and this is the crux of the idea, is that we need the project memory to essentially act as a living, breathing, ever‑evolving documentation of a project. The whole goal of the project memory is to save me the trouble of documenting my repository. The brain would grow and evolve as features are added, modified, or updated. This happens from memory candidates created by the session memory agent, which curates the session memory and finds information relevant to the project, then creates candidates for the project memory pipeline to consume and use as hints to inspect the application and create documentation based on that hint. A good example would be this exact session. Let's say that the project memory layer would have been a working feature. This conversation would then, I assume, create a session memory that states that we have worked on the project memory layer and essentially had a conversation on the vision of the product. The agent that curates this specific session memory would note that this chunk of information is relevant as part of the documentation of the project memory, as it essentially explains the product as a whole. It would then create a candidate for this information to become durable documentation that explains how Myelin works as a product. It's literally the product shape. It would create this memory candidate and the auto‑maintenance functionality of the project memory, which will work similarly to how the session memory auto‑maintenance works, take that candidate and the agent with this as an input would explore the code base based on the curated memory and create documentation and record the product shape, because it makes sense to do so as part of the documentation where the memory candidate is: "The user has described his vision of the product. This is something that is worth turning into a product‑chip documentation as part of the project memory"
So with this assumption in mind, we can also assume that a page that explains how the SQLite database works, where it is stored per project or globally, how do we fetch data from it, the difference between the session memory entries and the project memory or personal memory or practice memory index entries, how do they differ and how do we query them? This would sit in a documentation about the SQLite usage within this project, which then, when I ask the original question, where is the SQLite database stored in myelin for a project session memory, the index would be able to return entries specifically for the SQLite, which will then be used by the agent to answer my question without having to explore the whole code base. That's the idea.
THAT'S the product.
That's my vision.
