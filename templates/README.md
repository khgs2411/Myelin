# Templates

This directory contains starter files for new project spaces.

- `pages/`: markdown page templates
- `state/`: JSON metadata templates

Agents should prefer cloning and filling these templates rather than inventing project structure from scratch.

The bootstrap pipeline itself is stage-driven and defined separately under:

- `agents/bootstrap/`
- `agents/bootstrap/_shared/`
